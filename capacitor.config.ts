import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coface.cofamove',
  appName: 'Cofamove',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
